--Como importamos modulos X
--data Quality = Qua String Int Float deriving (Eq, Show) que significa (Eq, Show)
--No entedemos bien que recibe quality en los parametros.
module Main ()
    where
import Point